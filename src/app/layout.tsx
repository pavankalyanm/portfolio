import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Pavan K M — Software Engineer",
  description:
    "Portfolio of Pavan K M — Software Engineer proficient in Java, Python, JavaScript, React, Gen AI, and cloud technologies.",
  openGraph: {
    title: "Pavan K M — Software Engineer",
    description:
      "Software Engineer specializing in Full-Stack, Gen AI, and Cloud. Explore my portfolio in Dev or HR mode.",
    type: "website",
  },
  icons: {
    icon: "/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="h-full">{children}</body>
    </html>
  );
}
